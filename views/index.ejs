<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>⚽ ビスタジア | ピカイチ投票でサッカーをもっと熱く！</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP">
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      background: linear-gradient(135deg, #e1f1ff, #f9fcff);
      margin: 0;
      padding: 0;
      color: #333;
      text-align: center;
    }
    .header {
      background-color: #0055a5;
      color: #ffffff;
      padding: 1.5rem;
      font-size: 2.2rem;
      font-weight: bold;
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }
    .container {
      max-width: 700px;
      margin: auto;
      padding: 1rem;
    }
    .league-select button {
      padding: 0.9rem 1.5rem;
      margin: 0.4rem;
      font-size: 1.05rem;
      border-radius: 12px;
      border: none;
      color: #ffffff;
      background-color: #007aff;
      box-shadow: 0 3px 8px rgba(0,0,0,0.15);
      cursor: pointer;
      transition: transform 0.3s, background-color 0.3s;
    }
    .league-select button:hover {
      background-color: #0064d6;
      transform: translateY(-3px);
    }
    .match-card {
      background-color: #ffffff;
      border-radius: 16px;
      padding: 1.2rem;
      margin-top: 1rem;
      box-shadow: 0 6px 12px rgba(0,0,0,0.1);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .match-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 16px rgba(0,0,0,0.15);
    }
    .match-card a {
      text-decoration: none;
      color: #007aff;
      font-weight: bold;
      font-size: 1.1rem;
    }
    .footer {
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="header">⚽ ビスタジア ⚽<br><small style="font-size:1.2rem;">ピカイチ投票でサッカーを熱く盛り上げよう！</small></div>

  <div class="container">
    <div class="league-select">
      <button onclick="showLeague('J1')">J1リーグ</button>
      <button onclick="showLeague('J2')">J2リーグ</button>
      <button onclick="showLeague('J3')">J3リーグ</button>
      <button onclick="showLeague('WE')">WEリーグ</button>
    </div>

    <div id="matches"></div>
  </div>

  <div class="footer">
    🌟 ビスタジアで投票して推し選手を盛り上げよう！
  </div>

  <script>
    const matchesData = JSON.parse('<%- JSON.stringify(matches) %>');

    function showLeague(league) {
      const container = document.getElementById('matches');
      container.innerHTML = '';
      if(matchesData[league]) {
        for(const [section, games] of Object.entries(matchesData[league])) {
          games.forEach(game => {
            container.innerHTML += `
              <div class="match-card">
                <div style="font-weight:bold; font-size:1.1rem;">🏟️ ${section}</div>
                <div style="margin:0.5rem 0;">🔥 ${game.home} vs ${game.away} 🔥</div>
                <a href="/match/${game.id}">👉 今すぐ投票する！</a>
              </div>
            `;
          });
        }
      }
    }

    document.addEventListener('DOMContentLoaded', () => {
      showLeague('J1');
    });
  </script>

</body>
</html>
