<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>ピカイチ投票｜SNSサポの一票がヒーローを生む</title>
  <meta name="description" content="SNSから今すぐ1タップ！Jリーグ・WEリーグ全試合対応、ピカイチ投票で“今日一番輝いた選手”を決めよう。あなたの一票でサッカーが熱くなる！" />
  <meta property="og:title" content="ピカイチ投票｜SNSから1タップで推しをヒーローに" />
  <meta property="og:description" content="Jリーグサポの“本気の一票”で輝いた選手をピカイチに！SNS時代の離脱ゼロUX。" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bisutazia.com/" />
  <meta property="og:image" content="https://bisutazia.com/ogp.png" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="ピカイチ投票 | Jリーグ・WEリーグで一番輝いた選手に1タップ投票" />
  <meta name="twitter:description" content="SNSサポーターの一票がヒーローを生む！離脱ゼロUXの“ピカイチ投票”" />
  <meta name="twitter:image" content="https://bisutazia.com/ogp.png" />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary: #0366d6;
      --accent: #ffbe16;
      --danger: #ff4949;
      --bg: #f9fafb;
      --card-bg: #fff;
      --text: #222;
      --shadow: 0 2px 24px rgba(0,0,0,0.13);
      --glass: rgba(255,255,255,0.8);
    }
    html,body { height:100%; }
    body {
      margin: 0;
      font-family: 'Noto Sans JP',sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height:100vh;
      -webkit-tap-highlight-color:transparent;
      overflow-x:hidden;
      position:relative;
      font-size:17px;
    }
    /* ---- HERO ---- */
    .hero {
      margin-top:3vw;
      margin-bottom: 1.5em;
      padding: 2.4em 0.7em 1.4em 0.7em;
      border-radius: 2.2em;
      text-align: center;
      background: linear-gradient(120deg, var(--primary) 85%, var(--accent) 100%);
      color: #fff;
      box-shadow: var(--shadow);
      position:relative;
      overflow:hidden;
      max-width:480px;
      margin-left:auto; margin-right:auto;
      isolation:isolate;
    }
    .hero::before {
      content: "";
      position: absolute;
      top: -28px; left: 20%;
      width: 110px; height: 110px;
      background: radial-gradient(circle,#fff5b7bb 0%,transparent 80%);
      opacity: .55;
      z-index:0;
    }
    .hero h1 {
      font-size: 1.55em;
      margin: 0 0 0.34em 0;
      font-weight: 900;
      letter-spacing:0.025em;
      line-height:1.2;
      text-shadow:0 3px 16px #0366d655, 0 0 17px #ffbe1640;
      z-index:2; position:relative;
      display:inline-block;
      animation: fadeDown .78s cubic-bezier(.7,0,.38,1.22);
    }
    @keyframes fadeDown {
      0% { opacity:0; transform:translateY(-30px) scale(0.98);}
      100% { opacity:1; transform:translateY(0) scale(1);}
    }
    .hero .star-bounce {
      display:inline-block;
      font-size:1.3em;
      margin-left:0.08em;
      animation: pikapika 1.3s cubic-bezier(.77,0,.18,1.14) infinite;
      vertical-align:middle;
      filter:drop-shadow(0 0 11px #ffbe16aa);
    }
    @keyframes pikapika {
      0%,100% { transform:translateY(0) scale(1);}
      15% { transform:translateY(-9px) scale(1.14);}
      30% { transform:translateY(-2px) scale(1.02);}
      50% { transform:translateY(0) scale(1);}
    }
    .hero p {
      margin: 0.7em 0 1.3em 0;
      font-size: 1.09em;
      line-height:1.65;
      font-weight:500;
      z-index:2; position:relative;
      animation: fadeDown .9s .12s both cubic-bezier(.73,0,.2,1.18);
      letter-spacing:0.012em;
    }
    .timer {
      display: inline-block;
      background: var(--accent);
      color: var(--primary);
      font-size:1em;
      font-weight:bold;
      border-radius:8px;
      padding:0.38em 0.95em;
      margin-bottom: 1.17em;
      margin-top: 0.2em;
      box-shadow:0 0 12px #ffe0661c;
      letter-spacing:0.01em;
      animation: fadeDown .82s .21s both cubic-bezier(.73,0,.2,1.18);
    }
    /* ---- CTAボタン ---- */
    .cta-btn {
      display: block;
      width: 100%;
      background: linear-gradient(90deg,var(--accent) 65%,#fff1c0 100%);
      color: var(--primary);
      font-size: 1.24em;
      font-weight: 900;
      padding: 1.18em 0;
      border: none;
      border-radius: 99px;
      margin-top: 0.8em;
      margin-bottom: 0.13em;
      box-shadow: 0 7px 28px #ffe06633;
      transition: background 0.12s, color 0.14s, box-shadow 0.17s, transform 0.12s;
      letter-spacing: 0.03em;
      cursor:pointer;
      position:relative;
      overflow:hidden;
      animation: ctaPop .9s .19s both cubic-bezier(.77,0,.18,1.14);
      will-change:transform,box-shadow;
    }
    .cta-btn .star {
      display:inline-block;
      vertical-align:middle;
      margin-left:0.09em;
      animation: pikapika 1.8s cubic-bezier(.77,0,.18,1.14) infinite;
      filter:drop-shadow(0 0 10px #ffe066aa);
    }
    .cta-btn:active,.cta-btn:hover {
      background:linear-gradient(90deg,#ffe066 70%,#fffbe6 100%);
      color: var(--primary);
      transform: scale(1.04);
      box-shadow:0 0 24px #ffe066cc;
    }
    @keyframes ctaPop {
      0% { opacity:0; transform:scale(.96);}
      70% { opacity:.7; transform:scale(1.10);}
      100% { opacity:1; transform:scale(1);}
    }
    /* ---- ナッジ ---- */
    .catch-nudge {
      margin: 1.15em auto 0.28em;
      background: var(--glass);
      color: var(--primary);
      font-size: 1.06em;
      border-left: 7px solid var(--accent);
      padding: 0.78em 1.2em;
      border-radius: 12px;
      font-weight: bold;
      box-shadow:0 2px 19px rgba(0,0,0,0.03);
      line-height:1.5;
      text-align:center;
      max-width:440px;
      animation: fadeDown .9s .33s both cubic-bezier(.73,0,.2,1.18);
      isolation:isolate;
    }
    /* ---- 投票カード ---- */
    .match-list {
      margin: 1.28em 0 0 0;
      display: flex;
      flex-direction: column;
      gap: 1.01em;
      animation: fadeDown .7s .41s both cubic-bezier(.73,0,.2,1.18);
    }
    .match-card {
      background: var(--card-bg);
      border-radius: 1.2em;
      box-shadow: 0 3px 20px rgba(0,0,0,0.09);
      padding: 1.1em 0.5em 0.75em;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border-left: 6px solid var(--primary);
      position:relative;
      overflow:hidden;
      animation: cardPop .7s cubic-bezier(.68,-0.55,.27,1.55) both;
      will-change:transform,box-shadow;
      transition:box-shadow 0.14s,transform 0.13s;
    }
    @keyframes cardPop {
      0% {opacity:0;transform:scale(0.95);}
      100% {opacity:1;transform:scale(1);}
    }
    .match-card h3 {
      font-size: 1.13em;
      color: var(--primary);
      margin: 0.21em 0 0.41em;
      font-weight: bold;
      letter-spacing:0.01em;
      text-shadow:0 1px 7px #ffbe1630;
    }
    .match-card .vote-btn {
      background: linear-gradient(90deg,var(--accent) 65%,#fffbe6 100%);
      color: var(--primary);
      border: none;
      border-radius: 99px;
      padding: 0.97em 0;
      width: 97%;
      font-size: 1.09em;
      font-weight: 900;
      margin: 0.59em 0 0.1em 0;
      cursor: pointer;
      transition: background 0.12s, color 0.12s, box-shadow 0.11s, transform 0.13s;
      box-shadow:0 2px 8px #ffe06651;
      position:relative;
      overflow:hidden;
      letter-spacing:0.015em;
      animation: ctaPop .7s .51s both;
      will-change:transform,box-shadow;
    }
    .match-card .vote-btn .star {
      display:inline-block; vertical-align:middle; margin-left:0.07em;
      animation: pikapika 1.6s cubic-bezier(.77,0,.18,1.14) infinite;
    }
    .match-card .vote-btn:active,.match-card .vote-btn:hover {
      background:linear-gradient(90deg,#ffe066 75%,#fffbe6 100%);
      color: var(--primary);
      transform:scale(1.04);
      box-shadow:0 0 18px #ffe06690;
    }
    .info-tip {
      font-size: 0.97em;
      color: #555;
      margin: 1.08em 0 0.28em 0;
      text-align: center;
      animation: fadeDown .7s .69s both cubic-bezier(.73,0,.2,1.18);
    }
    /* ---- フローティング・ロゴ ---- */
    .logo-float {
      position: fixed;
      right:3vw;
      bottom:2.7vw;
      width: 25px; height: 25px;
      z-index:21;
      pointer-events: auto;
      user-select:none;
      opacity:0.71;
      animation: logoPop 2.2s cubic-bezier(.67,0,.36,1.13) infinite alternate;
      filter:drop-shadow(0 0 10px #ffe06633);
      transition:filter 0.13s;
    }
    @keyframes logoPop {
      0% { transform: translateY(0);}
      24% { transform: translateY(-7px) scale(1.13);}
      54% { transform: translateY(0);}
      100% { transform: translateY(0);}
    }
    .logo-float:active {
      filter:drop-shadow(0 0 22px #ffe066cc) brightness(1.11);
      animation-play-state: paused;
    }
    /* ---- レスポンシブ ---- */
    @media (max-width:480px){
      body { font-size:16px;}
      .hero{padding:1.6em 0.16em 0.8em;}
      .match-card{padding:0.55em 0.03em 0.65em;}
      .logo-float{width:17px;height:17px;}
    }
  </style>
</head>
<body>
  <!-- ピカイチスターはSVGやPNGで用意、例: https://www.svgrepo.com/svg/528642/star  -->
  <img src="/pikapika_star.svg" alt="ピカイチスター" class="pikapika" aria-hidden="true">
  <!-- ロゴ右下に小さく -->
  <img src="/logo.png" alt="ビスタジア ロゴ" class="logo-float" id="floatLogo"
    onclick="window.scrollTo({top:0,behavior:'smooth'})" tabindex="0" aria-label="ページトップに戻る" />
  <header>
    <img src="/logo.png" alt="ビスタジア ロゴ" class="logo-ani" />
    <nav id="leagues">
      <button onclick="showLeague('J1')" id="J1">J1</button>
      <button onclick="showLeague('J2')" id="J2">J2</button>
      <button onclick="showLeague('J3')" id="J3">J3</button>
      <button onclick="showLeague('WE')" id="WE">WE</button>
      <button onclick="showLeague('ルヴァンカップ')" id="ルヴァンカップ">ルヴァン杯</button>
      <button onclick="showLeague('その他')" id="その他">その他</button>
    </nav>
  </header>
  <main>
    <section class="hero">
      <h1>ピカイチ投票 <span class="star-bounce">✨</span></h1>
      <p>
        SNSサポーターが<br>
        <b>“一番輝いた選手”</b>に1タップ投票！<br>
        今しかできない、<span style="color:#fff;">あなたの“本気”を届けよう。</span>
      </p>
      <div class="timer" id="voteTimer">本日の投票 締切まで残り --時間--分</div>
      <button class="cta-btn" onclick="scrollToList()">
        ピカイチ投票する <span class="star">✨</span>
      </button>
    </section>
    <div class="catch-nudge">
      サポーターの投票が<br>
      “公式化”の推進力になる。<br>
      スマホ1タップ、離脱ゼロ！
    </div>
    <div class="match-list" id="matchList"></div>
    <div class="info-tip">
      投票後は <b>#ピカイチ投票 #ビスタジア</b> でSNS拡散！みんなの推し熱が未来を変える。
    </div>
  </main>
  <footer>
    非公式プロジェクトです（Jリーグ公式化を目指して運営中）<br>
    &copy; <script>document.write(new Date().getFullYear())</script> ビスタジア
  </footer>
  <script>
    // matches: { J1: { 節: { matches:[{id,home,away}] } }, ... }
    const matchesData = <%- JSON.stringify(matches) %>;

    function showLeague(league) {
      document.querySelectorAll('nav button').forEach(btn=>btn.classList.remove('active'));
      if(document.getElementById(league))document.getElementById(league).classList.add('active');
      const list = document.getElementById('matchList');
      list.innerHTML = '';
      if (league === 'その他') {
        window.location.href = '/match/other';
        return;
      }
      const secData = matchesData[league] || {};
      Object.entries(secData).forEach(([section, data]) => {
        data.matches.forEach(m => {
          const card = document.createElement('div');
          card.className = 'match-card';
          card.innerHTML = `
            <h3>${m.home} vs ${m.away}</h3>
            <button class="vote-btn" onclick="location.href='/match/${m.id}'">
              ピカイチ投票 <span class="star">✨</span>
            </button>
          `;
          list.appendChild(card);
        });
      });
      setTimeout(scrollToList, 0);
    }
    function scrollToList(){
      const list=document.getElementById('matchList');
      if(list){
        list.scrollIntoView({behavior:'smooth', block:'start'});
      }
    }
    function updateTimer(){
      const now = new Date();
      const close = new Date();
      close.setHours(23,59,59,999);
      const diff = close-now;
      if(diff>0){
        const h = String(Math.floor(diff/3600000)).padStart(2,"0");
        const m = String(Math.floor((diff%3600000)/60000)).padStart(2,"0");
        document.getElementById('voteTimer').textContent =
          `本日の投票 締切まで残り ${h}時間${m}分`;
      }else{
        document.getElementById('voteTimer').textContent = "本日の投票は締切ました";
      }
    }
    setInterval(updateTimer,1000); updateTimer();

    // 初期表示
    function getSNSLeague(){
      const u = new URL(window.location.href);
      return u.searchParams.get("league") || "J1";
    }
    window.onload = () => {
      showLeague(getSNSLeague());
      // ピカイチスターを1.2秒で消す（UX阻害しない）
      setTimeout(()=>{
        const pika = document.querySelector('.pikapika');
        if(pika) pika.style.display='none';
      },1200);
    };
    // 右下ロゴにタップアニメ追加
    document.addEventListener('DOMContentLoaded',()=>{
      const logo = document.getElementById('floatLogo');
      if(logo){
        logo.addEventListener('click',()=>{
          logo.style.transition='transform 0.22s';
          logo.style.transform='scale(1.19) rotate(16deg)';
          setTimeout(()=>{
            logo.style.transform='';
            logo.style.transition='';
          },180);
        });
      }
    });
  </script>
</body>
</html>
