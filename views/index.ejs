<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ビスタジア | サッカーサポーターが選ぶピカイチ投票</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP">
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      background-color: #f7fbff;
      margin: 0;
      padding: 0;
      color: #333;
      text-align: center;
    }
    .header {
      background-color: #003f87;
      color: #fff;
      padding: 1.2rem;
      font-size: 2rem;
      font-weight: bold;
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }
    .container {
      max-width: 650px;
      margin: auto;
      padding: 1rem;
    }
    .league-select button {
      padding: 0.8rem 1.2rem;
      margin: 0.4rem;
      font-size: 1rem;
      font-weight: bold;
      border-radius: 12px;
      border: none;
      color: #fff;
      background-color: #0078d4;
      box-shadow: 0 3px 8px rgba(0,0,0,0.12);
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .league-select button:hover {
      background-color: #005fa3;
      transform: translateY(-2px);
    }
    .match-card {
      background-color: #fff;
      border-radius: 14px;
      padding: 1rem;
      margin-top: 1rem;
      box-shadow: 0 4px 10px rgba(0,0,0,0.08);
      transition: all 0.3s ease;
    }
    .match-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 12px rgba(0,0,0,0.12);
    }
    .match-card a {
      text-decoration: none;
      color: #005bac;
      font-weight: bold;
      font-size: 1.05rem;
    }
    .footer {
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="header">⚽ ビスタジア ⚽<br><small style="font-size:1rem;">サポーターが選ぶピカイチ投票！</small></div>

  <div class="container">
    <div class="league-select">
      <button onclick="showLeague('J1')">J1リーグ</button>
      <button onclick="showLeague('J2')">J2リーグ</button>
      <button onclick="showLeague('J3')">J3リーグ</button>
      <button onclick="showLeague('WE')">WEリーグ</button>
    </div>

    <div id="matches"></div>
  </div>

  <div class="footer">
    📢 ビスタジアで選手を輝かせよう！
  </div>

  <script>
    const matchesData = <%- JSON.stringify(matches) %>;

    function showLeague(league) {
      const container = document.getElementById('matches');
      container.innerHTML = '';
      if(matchesData[league]) {
        for(const [section, games] of Object.entries(matchesData[league])) {
          games.forEach(game => {
            container.innerHTML += `
              <div class="match-card">
                <div><strong>🏟️ ${section}</strong></div>
                <div>🔥 ${game.home} vs ${game.away} 🔥</div>
                <a href="/match/${game.id}">👉 ピカイチを投票する</a>
              </div>
            `;
          });
        }
      }
    }

    document.addEventListener('DOMContentLoaded', () => {
      showLeague('J1');
    });
  </script>

</body>
</html>