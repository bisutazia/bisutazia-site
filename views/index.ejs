<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>ピカイチ投票｜Jリーグ・WEリーグ公式級サポ投票サイト</title>
  <meta name="description" content="SNSサポーターの熱投票で“今日一番輝いた選手”を決定！ピカイチ投票で推しをヒーローにしよう。リーグも一発選択、スマホ親指UI最適化。" />
  <meta property="og:title" content="ピカイチ投票｜Jサポ・WEサポ必見" />
  <meta property="og:description" content="Jリーグ・WEリーグの全試合対応。あなたの“熱い一票”がヒーローを生む。推しを讃えるピカイチ投票！" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bisutazia.com/" />
  <meta property="og:image" content="https://bisutazia.com/ogp.png" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="ピカイチ投票 | サポーターの一票が公式を動かす" />
  <meta name="twitter:description" content="スマホ1タップ、サポの熱でJリーグ・WEリーグの“ピカイチ”を決めよう！" />
  <meta name="twitter:image" content="https://bisutazia.com/ogp.png" />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@600;900&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary: #a68f46; /*ゴールド系*/
      --accent: #ffe066; /* 明るい金 */
      --bg: #f8f6ee;
      --card-bg: #fffbea;
      --text: #473c12;
      --shadow: 0 4px 32px rgba(166,143,70,0.15);
      --glass: rgba(255, 255, 240, 0.8);
    }
    html, body { height:100%; }
    body {
      margin: 0;
      font-family: 'Noto Sans JP', sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      -webkit-tap-highlight-color: transparent;
      overflow-x: hidden;
      font-size: 17px;
    }
    header {
      background: linear-gradient(90deg, #fffbe7 80%, #ffedb5 100%);
      color: var(--primary);
      padding: .5em 0 .1em 0;
      text-align: center;
      box-shadow: var(--shadow);
      border-bottom: 2px solid var(--primary);
      user-select: none;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .logo-ani {
      max-height: 34px;
      margin-bottom: .2em;
      margin-top: .2em;
      filter: drop-shadow(0 0 8px #ffe06680);
      border-radius: 10px;
      background: #fffbe7;
      padding: 4px 14px 4px 10px;
      display: inline-block;
      box-shadow: 0 2px 7px #ffe06638;
      animation: fadeDown .8s cubic-bezier(.7,0,.38,1.22);
    }
    @keyframes fadeDown {
      0% { opacity:0; transform:translateY(-18px);}
      100% { opacity:1; transform:translateY(0);}
    }
    .hero {
      margin-top: 1.2em;
      margin-bottom: 1.2em;
      padding: 2.1em .7em 1.4em .7em;
      border-radius: 22px;
      text-align: center;
      background: linear-gradient(115deg, #ffe066 70%, #a68f46 110%);
      color: var(--primary);
      box-shadow: var(--shadow);
      position: relative;
      overflow: hidden;
      max-width: 480px;
      margin-left: auto; margin-right: auto;
      isolation: isolate;
    }
    .hero h1 {
      font-size: 1.7em;
      margin: 0 0 0.22em 0;
      font-weight: 900;
      letter-spacing: 0.025em;
      line-height: 1.13;
      text-shadow: 0 3px 16px #a68f4660, 0 0 17px #ffe06645;
      z-index: 2;
      position: relative;
      display: inline-block;
      background: linear-gradient(90deg, #a68f46 45%, #ffe066 90%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .hero .star-bounce {
      display: inline-block;
      font-size: 1.25em;
      margin-left: 0.08em;
      animation: starPop 1.1s cubic-bezier(.8,-0.6,.18,1.14) infinite;
      vertical-align: middle;
      filter: drop-shadow(0 0 11px #ffe066bb);
    }
    @keyframes starPop {
      0%,100% { transform: translateY(0) scale(1);}
      17% { transform: translateY(-10px) scale(1.15);}
      28% { transform: translateY(-2px) scale(1.02);}
      49% { transform: translateY(0) scale(1);}
    }
    .hero p {
      margin: 0.65em 0 0.95em 0;
      font-size: 1.13em;
      line-height: 1.6;
      font-weight: 600;
      z-index: 2;
      position: relative;
      letter-spacing: 0.015em;
      color: #7c6a23;
      background: none;
      text-shadow: 0 1px 0 #fff7c8, 0 2px 8px #ffe06640;
    }
    /* ---- League selector ---- */
    .league-selector {
      display: flex;
      justify-content: center;
      gap: 0.5em;
      margin: 1.1em 0 0.4em 0;
      flex-wrap: wrap;
      z-index: 20;
      position: relative;
    }
    .league-btn {
      background: linear-gradient(90deg, #ffe066 80%, #a68f46 120%);
      color: var(--primary);
      border: none;
      border-radius: 1.7em;
      font-size: 1.13em;
      font-weight: 900;
      padding: 0.75em 1.6em 0.75em 1.6em;
      box-shadow: 0 2px 16px #ffe06666;
      letter-spacing: 0.04em;
      cursor: pointer;
      outline: none;
      transition: background 0.14s, color 0.11s, transform 0.12s;
      margin: 0.12em 0.1em;
      border-bottom: 2px solid #a68f46;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      min-width: 90px;
      gap: 0.2em;
    }
    .league-btn.active, .league-btn:focus, .league-btn:active {
      background: linear-gradient(90deg, #fffbe7 70%, #ffe066 130%);
      color: #b19d54;
      transform: scale(1.09);
      border-bottom: 3px solid #a68f46;
    }
    .league-btn .icon {
      font-size: 1.22em;
      margin-right: 0.15em;
      vertical-align: middle;
      filter: drop-shadow(0 0 5px #ffe06677);
    }
    .timer {
      display: inline-block;
      background: #fffbe7;
      color: #a68f46;
      font-size: 1em;
      font-weight: bold;
      border-radius: 8px;
      padding: 0.35em 1em;
      margin-bottom: 1.19em;
      margin-top: 0.25em;
      box-shadow: 0 0 12px #ffe0661c;
      letter-spacing: 0.01em;
    }
    .cta-btn {
      display: block;
      width: 100%;
      background: linear-gradient(90deg,#ffe066 75%,#fffbe7 100%);
      color: #a68f46;
      font-size: 1.19em;
      font-weight: 900;
      padding: 1.18em 0;
      border: none;
      border-radius: 99px;
      margin-top: 0.85em;
      margin-bottom: 0.1em;
      box-shadow: 0 7px 22px #ffe06655;
      transition: background 0.13s, color 0.12s, box-shadow 0.14s, transform 0.13s;
      letter-spacing: 0.03em;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      will-change: transform, box-shadow;
    }
    .cta-btn .star {
      display: inline-block;
      vertical-align: middle;
      margin-left: 0.09em;
      animation: starPop 1.6s cubic-bezier(.9,-0.7,.13,1.12) infinite;
      filter: drop-shadow(0 0 10px #ffe066bb);
    }
    .cta-btn:active, .cta-btn:hover {
      background: linear-gradient(90deg,#fffbe7 75%,#ffe066 100%);
      color: #a68f46;
      transform: scale(1.04);
      box-shadow: 0 0 22px #ffe066a5;
    }
    .catch-nudge {
      margin: 1.18em auto 0.28em;
      background: var(--glass);
      color: #b19d54;
      font-size: 1.05em;
      border-left: 7px solid var(--primary);
      padding: 0.81em 1.2em;
      border-radius: 13px;
      font-weight: bold;
      box-shadow: 0 2px 19px rgba(166,143,70,0.03);
      line-height: 1.5;
      text-align: center;
      max-width: 440px;
    }
    .match-list {
      margin: 1.3em 0 0 0;
      display: flex;
      flex-direction: column;
      gap: 1.13em;
    }
    .match-card {
      background: var(--card-bg);
      border-radius: 1.1em;
      box-shadow: 0 3px 18px rgba(166,143,70,0.08);
      padding: 1.05em 0.4em 0.67em;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border-left: 5px solid var(--primary);
      position: relative;
      overflow: hidden;
      transition: box-shadow 0.12s, transform 0.11s;
      will-change: transform, box-shadow;
    }
    .match-card h3 {
      font-size: 1.13em;
      color: #a68f46;
      margin: 0.13em 0 0.34em;
      font-weight: bold;
      letter-spacing: 0.01em;
      text-shadow: 0 1px 7px #ffe06655;
    }
    .match-card .vote-btn {
      background: linear-gradient(90deg,#ffe066 70%,#fffbe7 100%);
      color: #a68f46;
      border: none;
      border-radius: 99px;
      padding: 0.88em 0;
      width: 96%;
      font-size: 1.12em;
      font-weight: 900;
      margin: 0.57em 0 0.1em 0;
      cursor: pointer;
      transition: background 0.13s, color 0.11s, box-shadow 0.11s, transform 0.11s;
      box-shadow: 0 2px 7px #ffe06666;
      letter-spacing: 0.01em;
      will-change: transform, box-shadow;
    }
    .match-card .vote-btn .star {
      display: inline-block;
      vertical-align: middle;
      margin-left: 0.07em;
      animation: starPop 1.2s cubic-bezier(.8,-0.6,.18,1.14) infinite;
    }
    .match-card .vote-btn:active, .match-card .vote-btn:hover {
      background: linear-gradient(90deg,#fffbe7 60%,#ffe066 100%);
      color: #a68f46;
      transform: scale(1.03);
      box-shadow: 0 0 14px #ffe066b8;
    }
    .info-tip {
      font-size: 0.98em;
      color: #7d6f37;
      margin: 1.04em 0 0.27em 0;
      text-align: center;
    }
    /* ---- Floating ロゴ最適化 ---- */
    .logo-float {
      position: fixed;
      right: 4vw;
      bottom: 3vw;
      width: 22px; height: 22px;
      z-index: 21;
      pointer-events: auto;
      user-select: none;
      opacity: 0.72;
      filter: drop-shadow(0 0 8px #ffe0662e);
      transition: filter 0.11s, transform 0.12s;
      border-radius: 5px;
      background: #fffbe7;
      box-shadow: 0 2px 8px #ffe06635;
    }
    .logo-float:active {
      filter: drop-shadow(0 0 20px #ffe066cc) brightness(1.08);
      transform: scale(1.1) rotate(-9deg);
    }
    @media (max-width:480px){
      body { font-size:15.3px;}
      .hero{padding:1.05em 0.1em 0.5em;}
      .league-selector{gap:0.32em;}
      .match-card{padding:0.51em 0.03em 0.52em;}
      .logo-float{width:17px;height:17px;}
    }
  </style>
</head>
<body>
  <img src="/logo.png" alt="ビスタジア ロゴ" class="logo-float" id="floatLogo"
    onclick="window.scrollTo({top:0,behavior:'smooth'})" tabindex="0" aria-label="ページトップに戻る" />
  <header>
    <img src="/logo.png" alt="ビスタジア ロゴ" class="logo-ani" />
  </header>
  <main>
    <section class="hero">
      <h1>ピカイチ投票 <span class="star-bounce">✨</span></h1>
      <div class="league-selector" id="leagueSelector">
        <button class="league-btn" id="J1" onclick="showLeague('J1')"><span class="icon">🥇</span>J1</button>
        <button class="league-btn" id="J2" onclick="showLeague('J2')"><span class="icon">🥈</span>J2</button>
        <button class="league-btn" id="J3" onclick="showLeague('J3')"><span class="icon">🥉</span>J3</button>
        <button class="league-btn" id="WE" onclick="showLeague('WE')"><span class="icon">👑</span>WE</button>
        <button class="league-btn" id="ルヴァンカップ" onclick="showLeague('ルヴァンカップ')"><span class="icon">🏆</span>ルヴァン杯</button>
        <button class="league-btn" id="その他" onclick="showLeague('その他')"><span class="icon">⚽</span>その他</button>
      </div>
      <p>
        今日の試合で一番輝いた選手は誰？<br>
        あなたの「推し」や「感動」を<br>
        <b>“1タップ”</b>で届けよう！<br>
        <span style="color:#a68f46; font-weight:600;">サポーターの投票で<br>“本日のヒーロー”が決まります。</span>
      </p>
      <div class="timer" id="voteTimer">本日の投票 締切まで残り --時間--分</div>
      <button class="cta-btn" onclick="scrollToList()">
        ピカイチ投票する <span class="star">✨</span>
      </button>
    </section>
    <div class="catch-nudge">
      投票はとてもシンプル！<br>
      <b>1. 上からリーグを選択<br>2. 試合を選んで“ピカイチ投票”</b><br>
      サポーターの声で<br>
      <span style="color:#a68f46;">推しを“ヒーロー”に！</span>
    </div>
    <div class="match-list" id="matchList"></div>
    <div class="info-tip">
      投票後は <b>#ピカイチ投票 #ビスタジア</b> をつけて<br>
      “推し”や“試合”をSNSで広げよう！
    </div>
  </main>
  <footer>
    非公式プロジェクトです（Jリーグ公式化を目指して運営中）<br>
    &copy; <script>document.write(new Date().getFullYear())</script> ビスタジア
  </footer>
  <script>
    const matchesData = <%- JSON.stringify(matches) %>;
    function showLeague(league) {
      document.querySelectorAll('.league-btn').forEach(btn=>btn.classList.remove('active'));
      if(document.getElementById(league))document.getElementById(league).classList.add('active');
      const list = document.getElementById('matchList');
      list.innerHTML = '';
      if (league === 'その他') {
        window.location.href = '/match/other';
        return;
      }
      const secData = matchesData[league] || {};
      Object.entries(secData).forEach(([section, data]) => {
        data.matches.forEach(m => {
          const card = document.createElement('div');
          card.className = 'match-card';
          card.innerHTML = `
            <h3>${m.home} vs ${m.away}</h3>
            <button class="vote-btn" onclick="location.href='/match/${m.id}'">
              ピカイチ投票 <span class="star">✨</span>
            </button>
          `;
          list.appendChild(card);
        });
      });
      setTimeout(scrollToList, 0);
    }
    function scrollToList(){
      const list=document.getElementById('matchList');
      if(list){
        list.scrollIntoView({behavior:'smooth', block:'start'});
      }
    }
    function updateTimer(){
      const now = new Date();
      const close = new Date();
      close.setHours(23,59,59,999);
      const diff = close-now;
      if(diff>0){
        const h = String(Math.floor(diff/3600000)).padStart(2,"0");
        const m = String(Math.floor((diff%3600000)/60000)).padStart(2,"0");
        document.getElementById('voteTimer').textContent =
          `本日の投票 締切まで残り ${h}時間${m}分`;
      }else{
        document.getElementById('voteTimer').textContent = "本日の投票は締切ました";
      }
    }
    setInterval(updateTimer,1000); updateTimer();
    function getSNSLeague(){
      const u = new URL(window.location.href);
      return u.searchParams.get("league") || "J1";
    }
    window.onload = () => {
      showLeague(getSNSLeague());
    };
    document.addEventListener('DOMContentLoaded',()=>{
      const logo = document.getElementById('floatLogo');
      if(logo){
        logo.addEventListener('click',()=>{
          logo.style.transition='transform 0.18s';
          logo.style.transform='scale(1.14) rotate(-9deg)';
          setTimeout(()=>{
            logo.style.transform='';
            logo.style.transition='';
          },160);
        });
      }
    });
  </script>
</body>
</html>
