<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>【推しをヒーローに】ビスタジア｜Jリーグ ピカイチ投票</title>
  <meta name="description" content="Jリーグ全カテゴリの“本日のヒーロー”をファンの投票で決定！今すぐ推し選手に一票。スマホでサクッと、あなたの応援がヒーロー誕生の瞬間に。" />
  <meta property="og:title" content="ビスタジア｜Jリーグ ピカイチ投票 | サポーターの一票がヒーローを生む" />
  <meta property="og:description" content="推し選手を“ヒーロー”に。Jリーグ投票はビスタジアで！今すぐスマホで応援しよう。" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bisutazia.com/" />
  <meta property="og:image" content="https://bisutazia.com/ogp.png" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="ビスタジア | Jリーグ ピカイチ投票" />
  <meta name="twitter:description" content="サポーターの応援で“今日のヒーロー”を決定。今すぐ投票で推しを後押し！" />
  <meta name="twitter:image" content="https://bisutazia.com/ogp.png" />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary: #004c99;
      --accent: #ff5722;
      --highlight: #ffe066;
      --bg: #f4f6fa;
      --card-bg: #fff;
      --text: #232b34;
      --shadow: 0 2px 12px rgba(0,0,0,0.09);
    }
    html,body { height:100%; }
    body {
      margin: 0; font-family: 'Noto Sans JP',sans-serif; background:var(--bg); color:var(--text);
      min-height:100vh; -webkit-tap-highlight-color:transparent;
    }
    header {
      background: var(--primary);
      color: #fff;
      padding: 0.6rem 0.5rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      box-shadow: var(--shadow);
      position:sticky; top:0; z-index:10;
    }
    header .logo { max-height:38px; margin-bottom:0.3em; }
    nav {
      width: 100%;
      display: flex;
      gap: 0.3rem;
      overflow-x: auto;
      padding-bottom: 0.4em;
      justify-content: center;
      margin: 0;
      background:transparent;
    }
    nav button {
      flex:1 1 110px;
      background: var(--accent);
      color: #fff;
      border: none;
      border-radius: 99px;
      font-size: 1.02rem;
      padding: 0.65em 1em;
      font-weight: 700;
      margin: 0.2em 0;
      cursor: pointer;
      transition: background 0.22s;
      min-width:90px;
    }
    nav button.active, nav button:active, nav button:focus {
      background: var(--highlight);
      color: var(--primary);
    }
    .container {
      max-width: 500px;
      margin: 0 auto;
      padding: 0 0.7em 1.2em;
    }
    .hero {
      margin-top:1em;
      margin-bottom: 0.8em;
      background: linear-gradient(120deg,#004c99 90%,#4ea7ff 100%);
      color: #fff;
      border-radius: 18px;
      padding: 2.1em 1em 1.1em 1em;
      text-align: center;
      box-shadow: var(--shadow);
    }
    .hero h1 {
      font-size: 1.42em;
      margin: 0 0 0.3em 0;
      font-weight: bold;
      letter-spacing:0.02em;
      line-height:1.25;
      text-shadow:0 2px 6px rgba(0,0,0,0.16);
    }
    .hero p {
      margin: 0.5em 0 1.15em 0;
      font-size: 1.05em;
      line-height:1.6;
      letter-spacing:0.01em;
      color: #fffbe7;
      font-weight:500;
    }
    .timer {
      background: var(--highlight);
      color: var(--primary);
      display: inline-block;
      font-size:1.03em;
      font-weight:bold;
      border-radius:8px;
      padding:0.4em 0.85em;
      margin-bottom: 1em;
      margin-top: 0.1em;
    }
    .cta-btn {
      display: block;
      width: 100%;
      background: var(--accent);
      color: #fff;
      font-size: 1.14em;
      font-weight: 700;
      padding: 1.1em 0;
      border: none;
      border-radius: 99px;
      margin-top: 0.7em;
      margin-bottom: 0.2em;
      box-shadow: 0 4px 16px rgba(255,87,34,0.12);
      transition: background 0.18s;
      letter-spacing: 0.01em;
      cursor:pointer;
    }
    .cta-btn:active,.cta-btn:hover {
      background: #ff8352;
      color: #fff;
    }
    .catch-nudge {
      margin: 1.2em auto 0.2em;
      background: #fff;
      color: var(--primary);
      font-size: 1.05em;
      border-left: 6px solid var(--accent);
      padding: 0.8em 1em;
      border-radius: 10px;
      font-weight: bold;
      box-shadow:0 2px 12px rgba(0,0,0,0.07);
      line-height:1.5;
    }
    .match-list {
      margin: 1.2em 0 0 0;
      display: flex;
      flex-direction: column;
      gap: 0.85em;
    }
    .match-card {
      background: var(--card-bg);
      border-radius: 16px;
      box-shadow: 0 2px 14px rgba(0,0,0,0.08);
      padding: 1.1em 0.5em 0.8em;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border-left: 5px solid var(--primary);
    }
    .match-card h3 {
      font-size: 1.12em;
      color: var(--primary);
      margin: 0.2em 0 0.35em;
      font-weight: bold;
    }
    .match-card .vote-btn {
      background: var(--accent);
      color: #fff;
      border: none;
      border-radius: 99px;
      padding: 0.8em 0;
      width: 94%;
      font-size: 1.04em;
      font-weight: bold;
      margin: 0.7em 0 0.1em 0;
      cursor: pointer;
      transition: background 0.2s;
      box-shadow:0 2px 9px rgba(0,76,153,0.11);
    }
    .match-card .vote-btn:active,.match-card .vote-btn:hover {
      background: #ff8352;
      color: #fff;
    }
    .info-tip {
      font-size: 0.92em;
      color: #555;
      margin: 0.9em 0 0.2em 0;
      text-align: center;
    }
    footer {
      margin: 2.6em 0 1em 0;
      font-size: 0.92em;
      color: #888;
      text-align:center;
    }
    @media (max-width:480px){
      .container { padding: 0 0.2em;}
      .hero{padding:1.4em 0.6em 0.8em;}
    }
  </style>
</head>
<body>
  <header>
    <img src="/logo.png" alt="ビスタジア ロゴ" class="logo" />
    <nav id="leagues">
      <button onclick="showLeague('J1')" id="J1">J1</button>
      <button onclick="showLeague('J2')" id="J2">J2</button>
      <button onclick="showLeague('J3')" id="J3">J3</button>
      <button onclick="showLeague('WE')" id="WE">WE</button>
      <button onclick="showLeague('ルヴァンカップ')" id="ルヴァンカップ">ルヴァン杯</button>
      <button onclick="showLeague('その他')" id="その他">その他</button>
    </nav>
  </header>
  <main class="container">
    <section class="hero">
      <h1>推しを“ヒーロー”にできるのは<br>サポーターの一票だけ</h1>
      <p>Jリーグ全カテゴリ対応！<br>あなたの熱い投票が<br>“今日のヒーロー”を決める。</p>
      <div class="timer" id="voteTimer">本日の投票 締切まで残り --時間--分</div>
      <button class="cta-btn" onclick="scrollToList()">今すぐ投票する</button>
    </section>
    <div class="catch-nudge">
      あなたの投票が「公式化」の追い風になる。<br>
      1分で完了、迷わず推しをヒーローに！
    </div>
    <div class="match-list" id="matchList"></div>
    <div class="info-tip">
      投票後、Xで推し選手や #ビスタジア でシェアしよう！サポの声が広がるほど公式化が近づく！
    </div>
  </main>
  <footer>
    非公式プロジェクトです（Jリーグ公式連携を目指して運営中）<br>
    &copy; <script>document.write(new Date().getFullYear())</script> ビスタジア
  </footer>
  <script>
    // ---- 設定データ: matches = { J1: { 節: { matches:[{id,home,away}] } }, ... }
    const matchesData = <%- JSON.stringify(matches) %>;

    function showLeague(league) {
      document.querySelectorAll('nav button').forEach(btn=>btn.classList.remove('active'));
      if(document.getElementById(league))document.getElementById(league).classList.add('active');
      const list = document.getElementById('matchList');
      list.innerHTML = '';
      if (league === 'その他') {
        window.location.href = '/match/other';
        return;
      }
      const secData = matchesData[league] || {};
      Object.entries(secData).forEach(([section, data]) => {
        data.matches.forEach(m => {
          const card = document.createElement('div');
          card.className = 'match-card';
          card.innerHTML = `
            <h3>${m.home} vs ${m.away}</h3>
            <button class="vote-btn" onclick="location.href='/match/${m.id}'">この試合に投票する</button>
          `;
          list.appendChild(card);
        });
      });
      // スクロールで見せたい場合
      setTimeout(scrollToList, 0);
    }
    // スマホで投票エリアまで自動スクロール
    function scrollToList(){
      const list=document.getElementById('matchList');
      if(list){
        list.scrollIntoView({behavior:'smooth'});
      }
    }
    // 限定感: 締切までのカウントダウン
    function updateTimer(){
      const now = new Date();
      const close = new Date();
      close.setHours(23,59,59,999);
      const diff = close-now;
      if(diff>0){
        const h = String(Math.floor(diff/3600000)).padStart(2,"0");
        const m = String(Math.floor((diff%3600000)/60000)).padStart(2,"0");
        document.getElementById('voteTimer').textContent =
          `本日の投票 締切まで残り ${h}時間${m}分`;
      }else{
        document.getElementById('voteTimer').textContent = "本日の投票は締切ました";
      }
    }
    setInterval(updateTimer,1000); updateTimer();

    // 初期表示
    function getSNSLeague(){
      const u = new URL(window.location.href);
      return u.searchParams.get("league") || "J1";
    }
    window.onload = () => showLeague(getSNSLeague());
  </script>
</body>
</html>
