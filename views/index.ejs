<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>⚽ ビスタジア | ピカイチ投票でサッカーをもっと熱く！</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP">
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      background: linear-gradient(to right, #e4f0ff, #f8fbff);
      margin: 0;
      padding: 0;
      color: #222;
      text-align: center;
    }
    .header {
      background-color: #004a99;
      color: #fff;
      padding: 1.4rem;
      font-size: 2rem;
      font-weight: bold;
      box-shadow: 0 3px 12px rgba(0,0,0,0.2);
    }
    .container {
      max-width: 680px;
      margin: 0 auto;
      padding: 1rem;
    }
    .league-select button {
      padding: 0.8rem 1.4rem;
      margin: 0.5rem;
      font-size: 1rem;
      border-radius: 8px;
      border: none;
      color: #fff;
      background-color: #007bff;
      box-shadow: 0 3px 6px rgba(0,0,0,0.1);
      cursor: pointer;
      transition: all 0.2s ease-in-out;
    }
    .league-select button:hover {
      background-color: #005cbf;
      transform: scale(1.05);
    }
    .match-card {
      background-color: #fff;
      border-radius: 12px;
      padding: 1rem;
      margin: 0.8rem 0;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      transition: transform 0.2s ease;
    }
    .match-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 15px rgba(0,0,0,0.15);
    }
    .match-card a {
      text-decoration: none;
      color: #007bff;
      font-weight: bold;
      font-size: 1.05rem;
    }
    .footer {
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="header">⚽ ビスタジア ⚽<br><small style="font-size:1.1rem;">あなたの投票が選手を輝かせる！</small></div>

  <div class="container">
    <div class="league-select">
      <button onclick="showLeague('J1')">J1リーグ</button>
      <button onclick="showLeague('J2')">J2リーグ</button>
      <button onclick="showLeague('J3')">J3リーグ</button>
      <button onclick="showLeague('WE')">WEリーグ</button>
    </div>

    <div id="matches"></div>
  </div>

  <div class="footer">
    🌟 投票結果をSNSでシェアして、サッカーをさらに熱くしよう！🌟
  </div>

  <script>
    const matchesData = <%- JSON.stringify(matches) %>;

    function showLeague(league) {
      const container = document.getElementById('matches');
      container.innerHTML = '';

      if(matchesData[league]) {
        for(const [section, games] of Object.entries(matchesData[league])) {
          container.innerHTML += `<h3 style="color:#005cbf; margin-top:1rem;">🏟️ ${section}</h3>`;
          games.forEach(game => {
            container.innerHTML += `
              <div class="match-card">
                <div style="margin-bottom:0.5rem;">🔥 ${game.home} vs ${game.away} 🔥</div>
                <a href="/match/${game.id}">👉 今すぐピカイチに投票する！</a>
              </div>
            `;
          });
        }
      }
    }

    document.addEventListener('DOMContentLoaded', () => {
      showLeague('J1');
    });
  </script>

</body>
</html>
