<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>ピカイチ投票｜SNSサポの一票でヒーロー誕生</title>
  <meta name="description" content="Jリーグ・WEリーグ全カテゴリ対応。SNSサポーターが1タップで推しをヒーローにする投票体験。スマホ最適化・超わかりやすいリーグ選択ナビ搭載！" />
  <meta property="og:title" content="ピカイチ投票 | SNSサポの“推し投票”はここから" />
  <meta property="og:description" content="リーグ選択が一目でわかる！スマホで超簡単ピカイチ投票体験。" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="ピカイチ投票 | SNSで話題！推しに1タップ投票" />
  <meta name="twitter:description" content="Jリーグ・WEリーグ・ルヴァン杯・WEリーグ。スマホ下ナビで迷わず投票！" />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@500;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary: #0366d6;
      --accent: #ffbe16;
      --danger: #ff4949;
      --bg: #f8fafb;
      --card-bg: #fff;
      --text: #23272e;
      --shadow: 0 2px 24px rgba(0,0,0,0.13);
      --j1: #0366d6;   /* J1ブルー */
      --j2: #39b54a;   /* J2グリーン */
      --j3: #7e57c2;   /* J3パープル */
      --we: #ff4893;   /* WEピンク */
      --cup: #ffbe16;  /* カップ黄色 */
      --other: #a2a2a2;
    }
    html,body { height:100%; }
    body {
      margin: 0;
      font-family: 'Noto Sans JP',sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height:100vh;
      -webkit-tap-highlight-color:transparent;
      overflow-x:hidden;
      position:relative;
      font-size:17px;
    }
    header {
      width:100vw;
      background: transparent;
      text-align: center;
      position:sticky; top:0; z-index:30;
      box-shadow: none;
      padding: 0.7em 0 0.1em 0;
    }
    .logo-ani {
      height:36px;
      width:auto;
      display:inline-block;
      margin-bottom:0.25em;
      filter: drop-shadow(0 0 7px #ffe06655);
      animation:none;
      transition: transform 0.18s;
    }
    .logo-ani:active { transform:scale(1.14) rotate(-6deg);}
    /* -------- HERO -------- */
    .hero {
      margin: 0.1em auto 1em auto;
      padding: 2.2em 0.7em 1.2em 0.7em;
      border-radius: 1.6em;
      text-align: center;
      background: linear-gradient(120deg, var(--primary) 85%, var(--accent) 100%);
      color: #fff;
      box-shadow: var(--shadow);
      position:relative;
      max-width:430px;
      isolation:isolate;
    }
    .hero h1 {
      font-size: 1.29em;
      margin: 0 0 0.22em 0;
      font-weight: 900;
      letter-spacing:0.018em;
      line-height:1.18;
      text-shadow:0 2px 11px #0366d644, 0 0 15px #ffbe1633;
      z-index:2; position:relative;
      display:inline-block;
      animation: fadeDown .7s cubic-bezier(.7,0,.38,1.22);
    }
    @keyframes fadeDown {
      0% { opacity:0; transform:translateY(-20px);}
      100% { opacity:1; transform:translateY(0);}
    }
    .hero .star-bounce {
      display:inline-block;
      font-size:1.13em;
      margin-left:0.09em;
      animation: starpulse 1.18s cubic-bezier(.77,0,.18,1.14) infinite;
      vertical-align:middle;
      filter:drop-shadow(0 0 11px #ffbe16aa);
    }
    @keyframes starpulse {
      0%,100% { transform:scale(1);}
      14% { transform:scale(1.25);}
      28% { transform:scale(1);}
    }
    .hero p {
      margin: 0.6em 0 1.2em 0;
      font-size: 1.08em;
      line-height:1.6;
      font-weight:500;
      z-index:2; position:relative;
      animation: fadeDown .9s .13s both cubic-bezier(.73,0,.2,1.18);
      letter-spacing:0.012em;
    }
    .timer {
      display: inline-block;
      background: var(--accent);
      color: var(--primary);
      font-size:1em;
      font-weight:bold;
      border-radius:8px;
      padding:0.33em 0.89em;
      margin-bottom: 1.05em;
      margin-top: 0.1em;
      box-shadow:0 0 12px #ffe06618;
      letter-spacing:0.01em;
      animation: fadeDown .83s .21s both cubic-bezier(.73,0,.2,1.18);
    }
    /* -------- CTAボタン -------- */
    .cta-btn {
      display: block;
      width: 100%;
      background: linear-gradient(90deg,var(--accent) 70%,#fff1c0 100%);
      color: var(--primary);
      font-size: 1.18em;
      font-weight: 900;
      padding: 1.11em 0;
      border: none;
      border-radius: 99px;
      margin-top: 0.7em;
      margin-bottom: 0.11em;
      box-shadow: 0 4px 22px #ffe06644;
      transition: background 0.12s, color 0.14s, box-shadow 0.17s, transform 0.12s;
      letter-spacing: 0.02em;
      cursor:pointer;
      position:relative;
      overflow:hidden;
      animation: ctaPop .88s .19s both cubic-bezier(.77,0,.18,1.14);
      will-change:transform,box-shadow;
    }
    .cta-btn .star {
      display:inline-block;
      vertical-align:middle;
      margin-left:0.09em;
      animation: starpulse 1.4s cubic-bezier(.77,0,.18,1.14) infinite;
      filter:drop-shadow(0 0 8px #ffe06677);
    }
    .cta-btn:active,.cta-btn:hover {
      background:linear-gradient(90deg,#ffe066 78%,#fffbe6 100%);
      color: var(--primary);
      transform: scale(1.05);
      box-shadow:0 0 24px #ffe066cc;
    }
    @keyframes ctaPop {
      0% { opacity:0; transform:scale(.93);}
      70% { opacity:.7; transform:scale(1.10);}
      100% { opacity:1; transform:scale(1);}
    }
    /* -------- ナッジ -------- */
    .catch-nudge {
      margin: 1.12em auto 0.22em;
      background: #fff;
      color: var(--primary);
      font-size: 1.01em;
      border-left: 7px solid var(--accent);
      padding: 0.7em 0.9em;
      border-radius: 11px;
      font-weight: bold;
      box-shadow:0 2px 15px rgba(0,0,0,0.02);
      line-height:1.5;
      text-align:center;
      max-width:400px;
      animation: fadeDown .8s .32s both cubic-bezier(.73,0,.2,1.18);
    }
    /* -------- 投票カード -------- */
    .match-list {
      margin: 1.13em 0 0 0;
      display: flex;
      flex-direction: column;
      gap: 0.98em;
      animation: fadeDown .7s .44s both cubic-bezier(.73,0,.2,1.18);
    }
    .match-card {
      background: var(--card-bg);
      border-radius: 1.13em;
      box-shadow: 0 3px 17px rgba(0,0,0,0.09);
      padding: 1.09em 0.48em 0.74em;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border-left: 5px solid var(--primary);
      position:relative;
      overflow:hidden;
      animation: cardPop .7s cubic-bezier(.68,-0.55,.27,1.55) both;
      will-change:transform,box-shadow;
      transition:box-shadow 0.14s,transform 0.13s;
    }
    @keyframes cardPop {
      0% {opacity:0;transform:scale(0.96);}
      100% {opacity:1;transform:scale(1);}
    }
    .match-card h3 {
      font-size: 1.09em;
      color: var(--primary);
      margin: 0.19em 0 0.38em;
      font-weight: bold;
      letter-spacing:0.01em;
      text-shadow:0 1px 7px #ffbe1630;
    }
    .match-card .vote-btn {
      background: linear-gradient(90deg,var(--accent) 67%,#fffbe6 100%);
      color: var(--primary);
      border: none;
      border-radius: 99px;
      padding: 0.95em 0;
      width: 97%;
      font-size: 1.07em;
      font-weight: 900;
      margin: 0.59em 0 0.1em 0;
      cursor: pointer;
      transition: background 0.12s, color 0.12s, box-shadow 0.11s, transform 0.13s;
      box-shadow:0 2px 8px #ffe0663a;
      position:relative;
      overflow:hidden;
      letter-spacing:0.013em;
      animation: ctaPop .7s .54s both;
      will-change:transform,box-shadow;
    }
    .match-card .vote-btn .star {
      display:inline-block; vertical-align:middle; margin-left:0.07em;
      animation: starpulse 1.12s cubic-bezier(.77,0,.18,1.14) infinite;
    }
    .match-card .vote-btn:active,.match-card .vote-btn:hover {
      background:linear-gradient(90deg,#ffe066 75%,#fffbe6 100%);
      color: var(--primary);
      transform:scale(1.04);
      box-shadow:0 0 16px #ffe06699;
    }
    .info-tip {
      font-size: 0.97em;
      color: #555;
      margin: 1.01em 0 0.25em 0;
      text-align: center;
      animation: fadeDown .7s .71s both cubic-bezier(.73,0,.2,1.18);
    }
    /* -------- 下部リーグナビ -------- */
    .bottom-nav {
      position:fixed;
      bottom:0; left:0; width:100vw;
      height:62px;
      background: #fff;
      border-top: 1px solid #eee;
      display:flex;
      justify-content:space-around;
      align-items:center;
      z-index:50;
      box-shadow:0 -2px 20px #0366d611;
    }
    .nav-btn {
      flex:1 1 0;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      font-size: 0.94em;
      font-weight: 700;
      color: #bbb;
      background: none;
      border: none;
      border-radius: 8px;
      padding: 0.25em 0 0.06em 0;
      transition: color 0.12s, background 0.13s;
      min-width:54px;
      cursor:pointer;
      outline:none;
      position:relative;
      user-select:none;
      height:54px;
    }
    .nav-btn.active {
      color: var(--primary);
      background: #f2faff;
      font-size:1.06em;
    }
    .nav-btn .icon {
      font-size:1.4em;
      margin-bottom:0.08em;
      filter:drop-shadow(0 1px 6px #eee3);
    }
    .nav-btn.j1.active   { color:var(--j1);}
    .nav-btn.j2.active   { color:var(--j2);}
    .nav-btn.j3.active   { color:var(--j3);}
    .nav-btn.we.active   { color:var(--we);}
    .nav-btn.cup.active  { color:var(--cup);}
    .nav-btn.other.active{ color:var(--other);}
    @media (max-width:480px){
      body { font-size:16px;}
      .hero{padding:1.4em 0.1em 0.85em;}
      .match-card{padding:0.47em 0.01em 0.54em;}
      .bottom-nav{height:52px;}
      .nav-btn{font-size:0.91em;min-width:42px;height:48px;}
      .nav-btn .icon{font-size:1.1em;}
      .logo-ani{height:26px;}
    }
  </style>
</head>
<body>
  <header>
    <img src="/logo.png" alt="ビスタジア ロゴ" class="logo-ani" />
  </header>
  <main>
    <section class="hero">
      <h1>ピカイチ投票 <span class="star-bounce">✨</span></h1>
      <p>
        サポーターがSNSで1タップ！<br>
        <b>“一番輝いた選手”</b>に投票しよう。
      </p>
      <div class="timer" id="voteTimer">本日の投票 締切まで残り --時間--分</div>
      <button class="cta-btn" onclick="scrollToList()">
        ピカイチ投票する <span class="star">✨</span>
      </button>
    </section>
    <div class="catch-nudge">
      リーグを選択 → 試合を選択 → 投票！<br>
      <b>超シンプルな投票体験で離脱ゼロ。</b>
    </div>
    <div class="match-list" id="matchList"></div>
    <div class="info-tip">
      投票後は #ピカイチ投票 #ビスタジア でSNS拡散しよう！
    </div>
  </main>
  <!-- スマホ下部リーグナビゲーション -->
  <nav class="bottom-nav">
    <button class="nav-btn j1 active"   id="navJ1"   onclick="navLeague('J1')">
      <span class="icon">⚽️</span>J1
    </button>
    <button class="nav-btn j2"   id="navJ2"   onclick="navLeague('J2')">
      <span class="icon">🟢</span>J2
    </button>
    <button class="nav-btn j3"   id="navJ3"   onclick="navLeague('J3')">
      <span class="icon">🟣</span>J3
    </button>
    <button class="nav-btn we"   id="navWE"   onclick="navLeague('WE')">
      <span class="icon">💖</span>WE
    </button>
    <button class="nav-btn cup"  id="navCup"  onclick="navLeague('ルヴァンカップ')">
      <span class="icon">🏆</span>杯
    </button>
    <button class="nav-btn other" id="navOther" onclick="navLeague('その他')">
      <span class="icon">⋯</span>他
    </button>
  </nav>
  <footer style="padding-bottom:70px;">
    非公式プロジェクトです（Jリーグ公式化を目指して運営中）<br>
    &copy; <script>document.write(new Date().getFullYear())</script> ビスタジア
  </footer>
  <script>
    const matchesData = <%- JSON.stringify(matches) %>;
    // 下部ナビ→リーグ切替
    function navLeague(league) {
      document.querySelectorAll('.nav-btn').forEach(btn=>btn.classList.remove('active'));
      if(league==='J1')      document.getElementById('navJ1').classList.add('active');
      else if(league==='J2') document.getElementById('navJ2').classList.add('active');
      else if(league==='J3') document.getElementById('navJ3').classList.add('active');
      else if(league==='WE') document.getElementById('navWE').classList.add('active');
      else if(league==='ルヴァンカップ') document.getElementById('navCup').classList.add('active');
      else if(league==='その他') document.getElementById('navOther').classList.add('active');
      showLeague(league);
      setTimeout(scrollToList, 0);
    }
    function showLeague(league) {
      const list = document.getElementById('matchList');
      list.innerHTML = '';
      if (league === 'その他') {
        window.location.href = '/match/other';
        return;
      }
      const secData = matchesData[league] || {};
      Object.entries(secData).forEach(([section, data]) => {
        data.matches.forEach(m => {
          const card = document.createElement('div');
          card.className = 'match-card';
          card.innerHTML = `
            <h3>${m.home} vs ${m.away}</h3>
            <button class="vote-btn" onclick="location.href='/match/${m.id}'">
              ピカイチ投票 <span class="star">✨</span>
            </button>
          `;
          list.appendChild(card);
        });
      });
    }
    function scrollToList(){
      const list=document.getElementById('matchList');
      if(list){
        list.scrollIntoView({behavior:'smooth', block:'start'});
      }
    }
    function updateTimer(){
      const now = new Date();
      const close = new Date();
      close.setHours(23,59,59,999);
      const diff = close-now;
      if(diff>0){
        const h = String(Math.floor(diff/3600000)).padStart(2,"0");
        const m = String(Math.floor((diff%3600000)/60000)).padStart(2,"0");
        document.getElementById('voteTimer').textContent =
          `本日の投票 締切まで残り ${h}時間${m}分`;
      }else{
        document.getElementById('voteTimer').textContent = "本日の投票は締切ました";
      }
    }
    setInterval(updateTimer,1000); updateTimer();

    // 初期表示
    function getSNSLeague(){
      const u = new URL(window.location.href);
      return u.searchParams.get("league") || "J1";
    }
    window.onload = () => {
      navLeague(getSNSLeague());
    };
  </script>
</body>
</html>
