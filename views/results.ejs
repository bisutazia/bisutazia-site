<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ピカイチ投票結果 | <%= match.home %> vs <%= match.away %></title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      background: linear-gradient(145deg, #f0faff, #ffffff);
      color: #222;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: auto;
      padding: 2rem 1rem;
      background-color: #fff;
      border-radius: 18px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      text-align: center;
    }
    h1 {
      font-size: 1.8rem;
      color: #114488;
      margin-bottom: 1rem;
    }
    .thankyou {
      font-size: 1.1rem;
      color: #4caf50;
      font-weight: bold;
      margin-bottom: 1.5rem;
    }
    canvas {
      margin: 2rem 0;
      width: 100%;
    }
    .social-share {
      margin-top: 2rem;
      padding: 1.5rem;
      background: linear-gradient(135deg, #f9f9f9, #e6f2ff);
      border-radius: 16px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }
    .social-share h2 {
      font-size: 1.3rem;
      color: #114488;
      margin-bottom: 1.2rem;
    }
    .share-block {
      margin-bottom: 1rem;
      text-align: left;
    }
    .share-block textarea {
      width: 100%;
      background: #eef6ff;
      border: 1px solid #ccc;
      border-radius: 10px;
      padding: 0.7em;
      font-size: 0.9em;
      line-height: 1.5em;
      resize: none;
    }
    .share-button {
      display: block;
      width: 100%;
      background-color: #1da1f2;
      color: white;
      padding: 1em;
      text-align: center;
      font-size: 1em;
      border-radius: 12px;
      font-weight: bold;
      text-decoration: none;
      margin-top: 1em;
      transition: background-color 0.3s, transform 0.3s;
    }
    .share-button:hover {
      background-color: #0d8ddb;
      transform: scale(1.03);
    }
    .insta-note {
      font-size: 0.85em;
      text-align: center;
      margin-top: 1em;
      color: #666;
    }
    .home-link {
      display: inline-block;
      margin-top: 2rem;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }
    .highlight {
      color: #d32f2f;
      font-weight: bold;
    }
  </style>
</head>
<body>
<div class="container">
  <h1>🏆 今節のピカイチ投票結果</h1>
  <p class="thankyou">✨ あなたの投票ありがとうございました！✨</p>

  <canvas id="voteChart"></canvas>

  <div class="social-share">
    <h2>📣 結果をシェアして他のサポーターも引き込もう！</h2>
    <% if (topHome) { %>
    <div class="share-block">
      <h3>🏠 <%= match.home %> のピカイチ： <span class="highlight"><%= topHome %></span></h3>
      <textarea readonly>#ピカイチ投票 <%= match.home %>のヒーローは「<%= topHome %>」！🏀🔥
https://bisutazia-site.onrender.com/match/<%= match.id %></textarea>
    </div>
    <% } %>
    <% if (topAway) { %>
    <div class="share-block">
      <h3>🇳 <%= match.away %> のピカイチ： <span class="highlight"><%= topAway %></span></h3>
      <textarea readonly>#ピカイチ投票 <%= match.away %>のヒーローは「<%= topAway %>」！🏀🔥
https://bisutazia-site.onrender.com/match/<%= match.id %></textarea>
    </div>
    <% } %>

    <a class="share-button" href="https://twitter.com/intent/tweet?text=%23ピカイチ投票%20<%= match.home %>vs<%= match.away %>%20結果はこちら%ef%bc%9a%20https://bisutazia-site.onrender.com/match/<%= match.id %>" target="_blank">
      🕊 Xで共有する
    </a>

    <p class="insta-note">📸 Instagramストーリーのスクショ投稿も歓迎！ #ピカイチ
    </p>
  </div>
  <a href="/" class="home-link">🏠 ホームに戻る</a>
</div>

<script>
  const ctx = document.getElementById('voteChart').getContext('2d');
  const raw = JSON.parse(document.getElementById('chartData').textContent);

  new Chart(ctx, {
    type: 'bar',
    data: {
      labels: Object.keys(raw.home),
      datasets: [
        {
          label: '<%= match.home %>',
          data: Object.values(raw.home),
          backgroundColor: '#1E90FF'
        },
        {
          label: '<%= match.away %>',
          data: Object.values(raw.away),
          backgroundColor: '#FF6384'
        }
      ]
    },
    options: {
      responsive: true,
      plugins: {
        legend: {
          position: 'top'
        },
        title: {
          display: true,
          text: '投票結果チャート'
        }
      },
      scales: {
        y: {
          beginAtZero: true
        }
      }
    }
  });
</script>

<script id="chartData" type="application/json">
  {"home": <%- JSON.stringify(homeVotes) %>, "away": <%- JSON.stringify(awayVotes) %>}
</script>

</body>
</html>
